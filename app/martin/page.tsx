import HomePage from '@/components/HomePage';

// The full story, moved off `/` when the homepage became the three-door gate.
// The component is unchanged — only its address is.
export default function MartinPage() {
  return <HomePage />;
}
